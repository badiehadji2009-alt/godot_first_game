name: "تصدير أندرويد تلقائي"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  export-android:
    name: "بناء ملف APK"
    runs-on: ubuntu-latest

    steps:
      - name: "تحميل الكود من المستودع"
        uses: actions/checkout@v4

      - name: "إعداد بيئة Godot"
        uses: firebelley/godot-export@v5.2.1
        with:
          godot_executable_download_url: https://downloads.tuxfamily.org/godotengine/4.2.1/Godot_v4.2.1-stable_linux_headless.64.zip
          godot_export_templates_download_url: https://downloads.tuxfamily.org/godotengine/4.2.1/Godot_v4.2.1-stable_export_templates.tpz
          relative_project_path: ./
          archive_export_output: true
          # 🛑 تأكد أن اسم الإعداد في Godot هو Android تماماً
          export_preset_name: Android
          # سيتم حفظ الملف في هذا المسار داخل GitHub
          export_path: build/android/NewGameProject.apk

      - name: "رفع ملف الـ APK الناتج"
        uses: actions/upload-artifact@v4
        with:
          name: Android-Build
          path: build/android/NewGameProject.apk